Goal

Create reusable card component.

Changes

Add base class:

.card

.card {
padding:32px;
border:1px solid var(--border-color);
border-radius:12px;
background:white;
box-shadow:var(--shadow-sm);
}

Replace duplicated card styles.
